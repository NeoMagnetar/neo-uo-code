# Skill File Audit

| Skill Title | Likely File / Class | Confirmed? | Notes |
|---|---|---|---|
| Alchemy | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Anatomy | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Animal Lore | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Animal Taming | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Archery | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Arms Lore | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Begging | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Blacksmithy | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Bowcraft / Fletching | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Bushido | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Camping | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Carpentry | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Cartography | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Chivalry | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Cooking | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Detect Hidden | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Discordance | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Evaluating Intelligence | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Fishing | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Focus | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Forensic Evaluation | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Healing | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Herding | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Hiding | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Imbuing | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Inscription | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Item Identification | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Lockpicking | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Lumberjacking | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Mace Fighting | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Magery | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Meditation | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Mining | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Musicianship | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Mysticism | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Necromancy | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Ninjitsu | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Parrying | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Peacemaking | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Poisoning | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Provocation | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Remove Trap | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Resisting Spells | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Snooping | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Spirit Speak | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Spellweaving | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Stealing | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Stealth | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Swordsmanship | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Tactics | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Tailoring | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Taste Identification | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Tinkering | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Tracking | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Throwing | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Veterinary | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |
| Wrestling | TODO: locate implementation file(s). | No | File path intentionally left unguessed until confirmed. |